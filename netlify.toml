[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "24"

[[headers]]
  for = "/*"
  [headers.values]
    Permissions-Policy = "camera=(self)"

[context.dev]
  command = "npm run dev"

[context.branch-deploy]
  command = "npm run build"
